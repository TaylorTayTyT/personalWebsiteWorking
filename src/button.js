import "./button.sass";
import {Button, Container} from "@mui/material";

function button() {
    <Container>
        <Button className="topBarButton"></Button>
    </Container>
}

export default button; 